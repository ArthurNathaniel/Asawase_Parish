document.addEventListener("DOMContentLoaded", function () {
  var options = {
    strings: ["75 Years Anniversary"],
    typeSpeed: 50,
    backSpeed: 50,
    backDelay: 4000,
    startDelay: 100,
    showCursor: true,
    cursorChar: "",
    // loop: true,
  };

  var typed = new Typed(".typed-text", options);
});
